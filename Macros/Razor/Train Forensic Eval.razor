# Train Forensic Eval by Jaseowns
# UO Unchained
while not dead
    if findtype "corpse" true as jorpse
        useskill 'ForensicEval'
        waitfortarget 500
        target jorpse    
    endif
    wait 500
endwhile